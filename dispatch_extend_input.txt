Extend skill_invoker.py and devin_cli_adapter.py to support focused context and correction-loop input.

Current files:
$(Get-Content C:\Users\<username>\OneDrive\Documents\Work\devin-orchestrator\workflow-engine\skill_invoker.py -Raw)
$(Get-Content C:\Users\<username>\OneDrive\Documents\Work\devin-orchestrator\workflow-engine\devin_cli_adapter.py -Raw)

Requirements from ORCHESTRATION-RUNBOOK.md:
1. Accept a focused context manifest (list of artifact paths to inject into worker dispatch)
2. Support correction-loop input: on retry (attempt > 1), injected context becomes [original_context, previous_output, correction-{stage}-{attempt-1}.md]
3. The worker should only receive the focused context, not Cascade's full context (preserves neutrality)
4. Add parameters to invoke_skill: focused_context (list of artifact paths), correction_artifact (optional path)
5. Update the prompt building to include only the focused context artifacts
6. If correction_artifact is provided, include it in the dispatch context

Extend skill_invoker.py and devin_cli_adapter.py to support these features.
