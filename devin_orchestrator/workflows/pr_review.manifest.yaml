name: pr_review
description: "Pull Request Review workflow - review-only workflow for evaluating pull requests"
version: 1.0.0
schema_version: 1
session_shape: pr_review

stages:
  - step: 0
    name: fetch_pr
    skill: requesting-code-review
    description: "Fetch PR details, diff, and description"
    required_artifacts: [pr_url]
    output_artifacts: [pr_details.md, diff.md]
    gate: none

  - step: 1
    name: review_spec_compliance
    skill: requesting-code-review
    description: "Review PR against spec and requirements"
    required_artifacts: [pr_details.md, diff.md]
    output_artifacts: [spec_review.md]
    gate: none

  - step: 2
    name: review_code_quality
    skill: swe-compliance
    description: "Review code quality, style, and best practices"
    required_artifacts: [diff.md, spec_review.md]
    output_artifacts: [quality_review.md]
    gate: none

  - step: 3
    name: compile_findings
    skill: requesting-code-review
    description: "Compile review findings by severity"
    required_artifacts: [spec_review.md, quality_review.md]
    output_artifacts: [review_findings.md]
    gate: g1_approval_decision

gates:
  - id: g1_approval_decision
    name: Approval Decision
    description: "Human approval decision (approve, request changes, or block)"
    type: human
