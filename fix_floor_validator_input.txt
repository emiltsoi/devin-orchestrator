Fix the validate_structural function in floor_validator.py to accept a list of artifact paths instead of a single Path.

Current floor_validator.py:
$(Get-Content C:\Users\<username>\OneDrive\Documents\Work\devin-orchestrator\workflow-engine\floor_validator.py -Raw)

Required change:
Modify validate_structural to accept List[Path] instead of single Path, and iterate over all artifacts to collect failures.

The function should:
1. Accept artifacts: List[Path] parameter
2. Iterate over each artifact in the list
3. Check each artifact for existence, non-emptiness, and placeholder patterns
4. Collect all failures across all artifacts
5. Return {"result": "PASS" if no failures else "FAIL", "failures": failures}

Apply this fix to make floor_validator.py compliant with the ORCHESTRATION-RUNBOOK.md contract.
